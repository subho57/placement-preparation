#include <stdio.h>
main()
{
        int n,i,x,y;
		scanf("%d", &n);
        int l[n];
        int r[n];
        for(i=0;i<n;i++)
        {
            scanf("%d %d", &l[n], &r[n]);
        }
        scanf("%d %d", &x, &y);
        
}
